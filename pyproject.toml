[project]
name = "image-schema-llm"
version = "0.1.0"
description = "Structured image-schema LLM experiment utilities"
requires-python = ">=3.10"
dependencies = []

[tool.setuptools.package-dir]
"" = "src"

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"
